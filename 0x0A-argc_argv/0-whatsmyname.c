#include <stdio.h>
#include <string.h>

/**
 * main - entry point
 * @argc: command line argument; counter
 * @argv: one-dim array
 * Return: 0
 * noInvazion
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
